<div class="formulaire">
	{{ form_start(form) }}


	{{form_row(form.title)}}
	{{form_row(form.imgactivity)}}


	<button class="btn">{{ button_label|default('Save') }}</button>
	{{ form_end(form) }}

</div>

